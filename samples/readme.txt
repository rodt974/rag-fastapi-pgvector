Place .txt documents in this folder and run `python ingest.py samples/` to ingest them. Each .txt becomes a Document row, chunked into ~512-token pieces, embedded, and inserted into the chunks table.
